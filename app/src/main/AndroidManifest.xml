<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ly.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".BaseApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestScrollviewActivity" />
        <activity android:name=".TestParallaxActivity" />
        <activity android:name=".TestDargViewActivity" />
        <activity android:name=".TestRoundProgressActivity" />
        <activity android:name=".LuckPanActivity" />
        <activity android:name=".LuckPanActivity2" />
        <activity android:name=".MaterialEditActivity" />
        <activity android:name=".CustomDrawableActivity" />
        <activity android:name=".LikeActivity" />


    </application>

</manifest>